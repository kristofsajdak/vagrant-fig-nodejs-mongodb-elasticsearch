[supervisord]
nodaemon=true
loglevel = debug
logfile = /data/logs/supervisord-node-dev.log

[program:node-inspector]
command=node-inspector --web-port=8081 --save-live-edit=true

[program:nodejs]
command=forever --watchIgnore logs/** -v -d -a -w -c 'node --debug' app.js
