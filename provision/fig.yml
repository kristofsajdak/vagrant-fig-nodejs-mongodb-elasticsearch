web:
  build: .
  links:
      - db:db
  ports:
    - "8080:8080"
  expose:
    - "8080"
  environment:
    NODE_ENV: sandbox
  command: forever -w app.js
  volumes:
     - /src:/data
db:
  image: dockerfile/mongodb
  ports:
      - "27017:27017"