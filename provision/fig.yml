web:
  build: .
  links:
      - db:db
  ports:
    - "8080:8080"
    - "5858:5858"
  environment:
    PORT: 8080
  volumes:
     - /src:/data
db:
  image: dockerfile/mongodb
  ports:
      - "27017:27017"